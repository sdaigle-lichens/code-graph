# code-graph

Code graph extraction and query tool for TypeScript projects using ArangoDB.

## Install

```sh
pnpm i -g code-graph
```

## Dev setup

```sh
git clone <repo>
cd code-graph
pnpm install
pnpm build
pnpm link --global
code-graph --version
```
